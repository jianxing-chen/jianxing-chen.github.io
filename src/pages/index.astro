---
import BaseLayout from '@/components/layout/BaseLayout.astro';
import CoverSection from '@/components/interactive/CoverSection.astro';
import { useTranslations, getLocalizedPath } from '@/i18n/ui';
import { SITE_CONFIG } from '@/config/site';
import PublicationCard from '@/components/ui/PublicationCard.astro';
import BlogPostCard from '@/components/ui/BlogPostCard.astro';
import IconGithub from '@/components/ui/IconGithub.astro';
import IconOrcid from '@/components/ui/IconOrcid.astro';
import IconEmail from '@/components/ui/IconEmail.astro';
import { getCollection } from 'astro:content';

const lang = 'en';
const t = useTranslations(lang);

import papers from '@/content/publications/papers.json';
import type { Paper } from '@/types/publication';
const recentPapers = (papers as Paper[]).slice(0, 3);

const allPosts = await getCollection('blog', ({ data }) => data.lang === 'en' && !data.draft);
const recentPosts = allPosts.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()).slice(0, 3);

import galleryData from '@/data/gallery.json';
---

<BaseLayout title={t('site.title')} description={t('site.description')} lang={lang} isHomepage={true}>

  <!-- ═══ 固定封面背景层 ═══ -->
  <CoverSection photos={galleryData} lang={lang} />

  <!-- ═══ 可滚动内容层 ═══ -->
  <div id="page-content" class="home-page-content state-cover">

    <!-- Hero 容器：flex-start + spacer(flex-grow) 实现平滑过渡 -->
    <section class="hero-shell hero-container">
      <div class="hero-spacer"></div>
      <div id="hero-unified" class="hero-content hero-text-cover max-w-4xl mx-auto px-6 w-full">
        <div class="flex flex-col sm:flex-row items-center sm:items-center gap-6 sm:gap-7">
          <!-- Avatar -->
          <div class="shrink-0 sm:self-start sm:pt-1 flex w-24 sm:w-32 flex-col items-center">
            <img
              src={`${import.meta.env.BASE_URL}images/avatar.jpg`}
              alt="Avatar"
              width="128" height="128"
              class="w-24 h-24 sm:w-32 sm:h-32 rounded-full object-cover ring-2 ring-white/30 shadow-2xl"
            />
            <!-- Social links -->
            <div class="mt-3 flex items-center justify-center gap-3">
              <a href={SITE_CONFIG.social.github} target="_blank" rel="noopener noreferrer"
                 class="opacity-70 hover:opacity-100 transition-opacity" aria-label="GitHub">
                <IconGithub class="w-5 h-5" />
              </a>
              <a href={SITE_CONFIG.social.orcid} target="_blank" rel="noopener noreferrer"
                 class="opacity-70 hover:opacity-100 transition-opacity" aria-label="ORCID">
                <IconOrcid class="w-5 h-5" />
              </a>
              <a href={SITE_CONFIG.social.email}
                 class="opacity-70 hover:opacity-100 transition-opacity" aria-label="Email">
                <IconEmail class="w-5 h-5" />
              </a>
            </div>
          </div>
          <!-- Info -->
          <div class="hero-text-glass text-center sm:text-left pb-2 sm:pb-3 max-w-2xl">
            <h1 class="text-xl sm:text-2xl lg:text-3xl font-bold mb-0.5 drop-shadow-lg"
                style="font-family: 'Space Grotesk', 'Inter', sans-serif;">
              {SITE_CONFIG.author.en}
            </h1>
            <p class="text-sm sm:text-base font-medium mb-0.5 drop-shadow-md opacity-90">
              {SITE_CONFIG.position.en}
            </p>
            <p class="text-xs sm:text-sm drop-shadow-md opacity-75">
              {SITE_CONFIG.affiliation.en}
            </p>
            <p class="mt-3 text-sm sm:text-base font-medium opacity-90">
              {t('home.hero_tagline')}
            </p>
            <p class="mt-2 text-sm sm:text-base leading-relaxed opacity-80">
              {t('home.hero_description')}
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- 内容下方 -->
    <section id="content-below" class="content-below bg-background-light dark:bg-background-dark">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 py-12 sm:py-16 space-y-20 sm:space-y-28">

        <!-- ═══ Recent Publications ═══ -->
        <section class="reveal-on-scroll">
          <div class="flex items-baseline justify-between mb-8">
            <h2 class="text-2xl sm:text-3xl font-bold text-heading-light dark:text-heading-dark">
              {t('home.recent_publications')}
            </h2>
            <a href={getLocalizedPath('/publications', lang)}
               class="text-sm font-medium text-primary-light dark:text-primary-dark hover:underline underline-offset-4 whitespace-nowrap
                      transition-colors duration-200">
              {t('home.view_all')}
            </a>
          </div>
          <div class="grid gap-4 reveal-stagger">
            {recentPapers.map((paper) => (
              <PublicationCard paper={paper} lang={lang} />
            ))}
          </div>
        </section>

        <!-- ═══ Latest Posts ═══ -->
        <section class="reveal-on-scroll">
          <div class="flex items-baseline justify-between mb-8">
            <h2 class="text-2xl sm:text-3xl font-bold text-heading-light dark:text-heading-dark">
              {t('home.latest_posts')}
            </h2>
            <a href={getLocalizedPath('/blog', lang)}
               class="text-sm font-medium text-primary-light dark:text-primary-dark hover:underline underline-offset-4 whitespace-nowrap
                      transition-colors duration-200">
              {t('home.view_all')}
            </a>
          </div>
          <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 reveal-stagger">
            {recentPosts.map((post) => (
              <BlogPostCard post={post} lang={lang} />
            ))}
          </div>
        </section>

      </div>
    </section>

  </div>
</BaseLayout>
